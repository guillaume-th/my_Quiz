{% extends 'base.html.twig' %}

{% block title %}HistoriqueQuizz{% endblock %}

{% block body %}
    <h1>HistoriqueQuizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historique_quizz.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ historique_quizz.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('historique_quizz_index') }}">back to list</a>

    <a href="{{ path('historique_quizz_edit', {'id': historique_quizz.id}) }}">edit</a>

    {{ include('historique_quizz/_delete_form.html.twig') }}
{% endblock %}
